should = require('chai').should();

var i, number, rules, should, speak;

fizzbuzz = function(number) {
  return 0;
};

describe('fizzbuzz tests', function() {
  it('fizzbuzz of 1 should be 1', function() {
    return fizzbuzz(1).should.equal(1);
  });
});
